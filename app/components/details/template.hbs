<NavBar />
<div class="body">
  <Header />
  {{#if (and this.model this.users) }}
    <Details::Kudos @idea={{this.model}} @users={{this.users}} />
  {{/if}}
  <div class="details-wrapper">
    <div class="card">
      <h1 class="flexible">{{this.model.title}}</h1>
      <img alt="project img" src="{{this.model.imageURL}}" class="rounded">
      <div class="description">
        <p>{{this.model.description}}</p>
      </div>
      <div class="details">
        Number of kudos: <span>{{this.model.numberOfKudos}}</span>
      </div>
      <div class="details">
        Author: <span>
          {{#if (and this.session.isAuthenticated this.otherUser) }}
            <LinkTo @route="user-profile" @model={{this.otherUser.id}}>
              {{this.otherUser.name}}
            </LinkTo>
          {{else}}
            {{this.otherUser.name}}
          {{/if}}
        </span> 
      </div>
    </div>
  </div>
  <Details::Comments @postID={{this.model.id}}/>
</div>
